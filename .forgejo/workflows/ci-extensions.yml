name: "CI: Extensions"

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]
    branches: [main]
    paths:
      - "agent-runner/**"
      - "deno-runner/**"
      - "issue-lifecycle/**"
      - "ssh/**"
      - "kubernetes/**"
      - "workflows/**"
      - "cve/**"
      - "software-factory/**"
      - "container-image/**"
      - "git/**"
      - "typesafe-ai/**"

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      agent-runner: ${{ steps.filter.outputs.agent-runner }}
      deno-runner: ${{ steps.filter.outputs.deno-runner }}
      issue-lifecycle: ${{ steps.filter.outputs.issue-lifecycle }}
      ssh: ${{ steps.filter.outputs.ssh }}
      kubernetes: ${{ steps.filter.outputs.kubernetes }}
      workflows: ${{ steps.filter.outputs.workflows }}
      cve: ${{ steps.filter.outputs.cve }}
      software-factory: ${{ steps.filter.outputs.software-factory }}
      container-image: ${{ steps.filter.outputs.container-image }}
      git: ${{ steps.filter.outputs.git }}
      typesafe-ai: ${{ steps.filter.outputs.typesafe-ai }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - id: filter
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          CHANGED=$(git diff --name-only "${BASE_SHA}...${HEAD_SHA}")
          check_path() {
            local name=$1; shift; local found=false
            for pattern in "$@"; do
              if echo "$CHANGED" | grep -q "^${pattern}"; then found=true; break; fi
            done
            echo "${name}=${found}" >> $GITHUB_OUTPUT
          }
          check_path agent-runner "agent-runner/"
          check_path deno-runner "deno-runner/"
          check_path issue-lifecycle "issue-lifecycle/"
          check_path ssh "ssh/"
          check_path kubernetes "kubernetes/"
          check_path workflows "workflows/"
          check_path cve "cve/"
          check_path software-factory "software-factory/"
          check_path container-image "container-image/"
          check_path git "git/"
          check_path typesafe-ai "typesafe-ai/"

  agent-runner-check:
    name: agent-runner - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.agent-runner == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: agent-runner
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-read --allow-write --allow-env --allow-run --allow-sys --allow-net extensions/models/
          else
            deno check extensions/models/agent_runner.ts
          fi

  agent-runner-lockfile:
    name: agent-runner - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.agent-runner == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: agent-runner
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  deno-runner-check:
    name: deno-runner - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.deno-runner == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: deno-runner
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-read --allow-write --allow-env --allow-run --allow-sys --allow-net extensions/models/
          else
            deno check extensions/models/deno_runner.ts
          fi

  deno-runner-lockfile:
    name: deno-runner - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.deno-runner == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: deno-runner
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  issue-lifecycle-check:
    name: issue-lifecycle - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.issue-lifecycle == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: issue-lifecycle
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-read --allow-write --allow-env --allow-net --allow-sys extensions/models/
          else
            deno check extensions/models/issue_lifecycle.ts
          fi

  issue-lifecycle-lockfile:
    name: issue-lifecycle - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.issue-lifecycle == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: issue-lifecycle
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  ssh-check:
    name: ssh - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.ssh == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: ssh
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-read --allow-write --allow-env --allow-run --allow-sys extensions/models/
          else
            deno check extensions/models/ssh.ts
          fi

  ssh-lockfile:
    name: ssh - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.ssh == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ssh
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  kubernetes-check:
    name: kubernetes - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.kubernetes == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: kubernetes
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-env --allow-net --allow-read --allow-write --allow-run=openssl --allow-sys extensions/models/
          else
            deno check extensions/models/*.ts
          fi

  kubernetes-lockfile:
    name: kubernetes - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.kubernetes == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: kubernetes
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  workflows-check:
    name: workflows/${{ matrix.workflow }} - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        workflow: [s3-bootstrap, gcs-bootstrap]
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: workflows/${{ matrix.workflow }}
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-read --allow-write --allow-env --allow-net --allow-sys extensions/models/
          else
            deno check extensions/models/*.ts
          fi

  workflows-lockfile:
    name: workflows/${{ matrix.workflow }} - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        workflow: [s3-bootstrap, gcs-bootstrap]
    defaults:
      run:
        working-directory: workflows/${{ matrix.workflow }}
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  cve-check:
    name: cve/${{ matrix.cve }} - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.cve == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        cve: [dirtyfrag, mini-shai-hulud, researcher]
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: cve/${{ matrix.cve }}
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/ extensions/reports/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/ extensions/reports/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-read --allow-write --allow-env --allow-net --allow-sys extensions/models/ extensions/reports/
          else
            deno check extensions/models/*.ts extensions/reports/*.ts
          fi

  cve-lockfile:
    name: cve/${{ matrix.cve }} - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.cve == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        cve: [dirtyfrag, mini-shai-hulud, researcher]
    defaults:
      run:
        working-directory: cve/${{ matrix.cve }}
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  software-factory-check:
    name: software-factory - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.software-factory == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: software-factory
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/ extensions/reports/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/ extensions/reports/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-read --allow-write --allow-env --allow-net --allow-sys extensions/models/ extensions/reports/
          else
            deno check extensions/models/*.ts extensions/models/_lib/*.ts extensions/reports/*.ts
          fi

  software-factory-lockfile:
    name: software-factory - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.software-factory == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: software-factory
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  container-image-check:
    name: container-image - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.container-image == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: container-image
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-env --allow-read --allow-run --allow-sys extensions/models/
          else
            deno check extensions/models/container_image.ts
          fi

  container-image-lockfile:
    name: container-image - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.container-image == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: container-image
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  git-check:
    name: git - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.git == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: git
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-read --allow-write --allow-env --allow-run --allow-sys extensions/models/
          else
            deno check extensions/models/git.ts
          fi

  git-lockfile:
    name: git - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.git == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: git
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  typesafe-ai-check:
    name: typesafe-ai - ${{ matrix.task }}
    needs: [changes]
    if: needs.changes.outputs.typesafe-ai == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [check, lint, fmt, test]
    defaults:
      run:
        working-directory: typesafe-ai
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: deno ${{ matrix.task }}
        run: |
          if [ "${{ matrix.task }}" = "fmt" ]; then
            deno fmt --check extensions/models/
          elif [ "${{ matrix.task }}" = "lint" ]; then
            deno lint extensions/models/
          elif [ "${{ matrix.task }}" = "test" ]; then
            deno test --allow-env --allow-net extensions/models/
          else
            deno check extensions/models/typesafe_ai.ts
          fi

  typesafe-ai-lockfile:
    name: typesafe-ai - lockfile up to date
    needs: [changes]
    if: needs.changes.outputs.typesafe-ai == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: typesafe-ai
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.8.x
      - name: Verify lockfile is up to date
        run: deno install --frozen

  gate:
    name: "Gate: Extensions"
    if: always()
    needs:
      [
        agent-runner-check,
        agent-runner-lockfile,
        deno-runner-check,
        deno-runner-lockfile,
        issue-lifecycle-check,
        issue-lifecycle-lockfile,
        ssh-check,
        ssh-lockfile,
        kubernetes-check,
        kubernetes-lockfile,
        workflows-check,
        workflows-lockfile,
        cve-check,
        cve-lockfile,
        software-factory-check,
        software-factory-lockfile,
        container-image-check,
        container-image-lockfile,
        git-check,
        git-lockfile,
        typesafe-ai-check,
        typesafe-ai-lockfile,
      ]
    runs-on: ubuntu-latest
    steps:
      - name: Check for failures
        run: |
          if [ "${{ contains(needs.*.result, 'failure') }}" = "true" ]; then
            echo "::error::Extension checks failed"
            exit 1
          fi
